## -*- coding: utf-8 -*-
##
##
## This file was autogenerated by python_on_wheels scaffolding
##
##
## date created: 	2012-07-20
##
## inherits the layout defined in pow.cfg as default_layout
## is there rendered as self.body()
##
<%!

	import sys,os 
	sys.path.append(os.path.abspath(os.path.join( os.path.dirname(os.path.abspath("./")), "../../lib" )) )
	import powlib
	import pow_html_helper as helper
	hidden_list = powlib.hidden_list
	
%>
<%inherit file="/${context.get('template')}"/>

<h2>Edit ${model.__table__} Form:</h2>
    <div class="niceform">
        <fieldset>
          <form action="./update" method="post" enctype="multipart/form-data" accept-charset="utf-8" class="well">
                <label for="title">Title:</label>
                <input type="text" name="title" value="${model.get("title")}"/>
                <label for="content">Content:</label>
                <textarea name="content" cols="50" rows="20">${model.get("content")}</textarea>
                <label for="image">Image:</label>
                <input name="image" type="file" size="50" maxlength="100000" accept="image/*">
                <p>
                    <img src="/static/img/blog/${model.get_image()}"/>
                </p>
            </p>
            ${helper.generate_hidden(model)}
            <p>
              <input type="submit" id="submit" value="Submit" />
            </p>
          </form>
        </fieldset>
        
    </div> <!-- Niceform -->
    <p>
        <i class="icon-fast-backward"></i><a href="/${model.modelname}/list">Back to the list</a>
    </p>